## 0.1.0

* Initial release.
* `MonthCalendar`, `WeekCalendar`, `DayCalendar`, and `TimelineCalendar` widgets,
  each taking a `List<CalendarEvent>` of events to display.
* `CalendarEvent` and `CalendarViewType` models.

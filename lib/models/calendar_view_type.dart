/// Identifies one of the four showcased calendar styles.
///
/// Deliberately free of display strings (name/description are a
/// presentation concern — see `screens/calendar_showcase_screen.dart`).
enum CalendarViewType { month, week, day, timeline }
